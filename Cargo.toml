[package]
name = "openlist-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.8", features = ["multipart"] }
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["io"] }
futures-util = "0.3"
# gzip/deflate：部分网盘 CDN（如蓝奏云）不协商直接返回压缩响应，
# 必须启用解压特性，否则 HTML/JSON 响应为乱码（Go net/http 自动处理，reqwest 需显式开启）
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls", "cookies", "gzip", "deflate", "multipart"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tower = "0.5"
tower-http = { version = "0.7", features = ["fs"] }
uuid = { version = "1", features = ["v4"] }
base64 = "0.23"
url = "2"
clap = { version = "4", features = ["derive"] }
rust-embed = "8"
regex = "1"
md-5 = "0.11"
sha1 = "0.11"
num-bigint = "0.5"
rsa = { version = "0.9", features = ["pem"] }
rand = "0.10"
hex = "0.4"
hmac = "0.13"
sha2 = "0.11"
async-trait = "0.1"
# SSH/SFTP：russh 0.63（自带 keys 模块，不再需要 russh-keys / ssh-key 直接依赖）
russh = "0.63"
russh-sftp = "3"
# 持久化：redb 嵌入式 KV 库（单文件），账号数据 AES-256-GCM 加密后落盘
redb = "4"
# 仅用于一次性迁移：redb 4 移除了升级 API（打开旧格式库只报 UpgradeRequired），
# 所以用 redb 2 读旧格式库、原样写进新建的 redb 4 库。见 config.rs::migrate_legacy_store
redb2 = { package = "redb", version = "2" }
aes-gcm = "0.11"
# AES-ECB：蓝奏云/飞鸡盘接口签名用（AES-128-ECB + PKCS7，手写填充）
aes = "0.9"
# 面板密码哈希：argon2id（PHC 串落库）。已在依赖树里（russh → ssh-key 带进来），
# 提成直接依赖不引入新下载；参数与并发约束见 src/password.rs
argon2 = "0.6"

[profile.release]
strip = "symbols"
debug = false
